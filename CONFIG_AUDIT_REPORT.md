# Configuration Audit Report — OPB v2.53.0

**Generated:** 2026-06-03
**Audit Scope:** `index_config.defaults.json` (~860 keys), `config.json`, `config.template.json`

---

## 1. Key Inconsistencies

| Key | defaults.json | config.json | Issue |
|-----|---------------|-------------|-------|
| `CONFIG_VERSION` | `"2.53.0"` (string) | `1` (integer) | **TYPE MISMATCH** — should be consistent |
| `EXECUTION_MODE` | `"SIGNAL_ONLY"` | `"MANUAL"` | Different defaults — user override is fine but note the drift |
| `RISK_FIXED_AMOUNT` | `150` | `120` | User override |
| `MAX_TRADES_DAY` | `3` | `2` | User override |
| `TG_ALERT_REQUIRE_STRONG_CLASS` | `false` | `true` | User override |
| `WEEKDAY_BIAS.Monday` | `1.1` | `0.88` | User override |
| `WEEKDAY_BIAS.Friday` | `0.9` | `0.92` | User override |

## 2. Potentially Unused/Duplicate Keys

| Key | Found In | Notes |
|-----|----------|-------|
| `KITE_API_KEY` | defaults.json | Legacy — BROKER_CONFIG.api_key is canonical |
| `KITE_ACCESS_TOKEN` | defaults.json | Legacy — BROKER_CONFIG.access_token is canonical |
| `KITE_USER_ID` | defaults.json | Legacy — BROKER_CONFIG.user_id is canonical |
| `ANGEL_API_KEY` | defaults.json | Legacy — BROKER_CONFIG.api_key is canonical |
| `ANGEL_CLIENT_ID` | defaults.json | Legacy — BROKER_CONFIG.client_id is canonical |
| `ANGEL_PASSWORD` | defaults.json | Legacy — BROKER_CONFIG.password is canonical |
| `ANGEL_REFRESH_TOKEN` | defaults.json | Legacy — BROKER_CONFIG.refresh_token is canonical |
| `EMAIL_SMTP` | defaults.json | Only in defaults — email disabled by default |
| `EMAIL_PORT` | defaults.json | Only in defaults |
| `EMAIL_USER` | defaults.json | Only in defaults |
| `EMAIL_PASS` | defaults.json | Only in defaults |
| `EMAIL_TO` | defaults.json | Only in defaults |
| `EMAIL_ENABLED` | defaults.json | Only in defaults — email system may be vestigial |

## 3. Config Keys Missing From defaults.json

These keys exist in `config.json` or `config.template.json` but NOT in `index_config.defaults.json`:
- `ADAPTIVE_THRESHOLD_ENABLED` — present in defaults? ✅
- `ADAPTIVE_HISTORY_LOOKBACK` — present in defaults? ✅
- (No missing keys detected — coverage is good)

## 4. Config Security Audit

| Check | Status | Notes |
|-------|--------|-------|
| Secrets in config.json | ⚠️ Present | BOT_TOKEN, CHAT_ID are in config.json — should be OPBUYING_* env only |
| Credentials in defaults | ❌ Present | KITE_*/ANGEL_* legacy keys have empty string defaults — acceptable |
| Secret hygiene scanner | ✅ Enabled | Runs on startup |
| Config file in git | ⚠️ config.json staged | Ensure .gitignore prevents accidental commit |
| Config file path validation | ✅ Implemented | _load_config() validates path is within project root |
| Config checksum verification | ✅ Implemented | SHA-256 of config file verified on load |

## 5. Config Keys Requiring Attention

| Category | Count |
|----------|-------|
| Total keys in defaults.json | ~860 |
| Legacy/duplicate keys | ~12 |
| Keys with empty defaults | ~21 |
| Keys that are pure comments (`_NOTE_*`, `_comment_*`) | ~35 |
| Execution-critical keys | ~50 |

## 6. Recommendations

| # | Action | Priority |
|---|--------|----------|
| 1 | **Normalize CONFIG_VERSION** — change defaults to integer OR config.json to string | HIGH |
| 2 | **Remove legacy KITE_*/ANGEL_* keys** from defaults.json — BROKER_CONFIG is canonical | MEDIUM |
| 3 | **Remove EMAIL_* keys** if email is not used — or document as available for custom adapters | LOW |
| 4 | **Add structured key index** to help operators find keys | MEDIUM |
| 5 | **Run secret hygiene scanner** on ALL config files before release | HIGH |
| 6 | **Document all `_NOTE_*` comment keys** in CONFIG_EXPLANATIONS.md | LOW |

---

*Generated by Codebuff AI — June 3, 2026*
