# OPB Index Options Buying Bot — Runtime Dependencies
# Install: pip install -r requirements.txt
# Python: 3.10 through 3.19 (enforced at startup)

# ── Core (required by all scripts) ───────────────────────────────────────────
jsonschema>=4.20
requests>=2.31.0
yfinance>=1.7.0
pandas>=2.0.0
numpy>=1.24.0

# ── Dashboard server ──────────────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0

# ── Phase 5 — ML Signal Classifier ───────────────────────────────────────────
# Required for core/ml_classifier.py (LightGBM win-probability scorer)
# Activates after ml_min_trades_to_train (default 50) trades accumulate
lightgbm>=4.7.0
scikit-learn>=1.3.0

# ── Phase 6 — PDF Report Generator ───────────────────────────────────────────
# Required for core/report_generator.py (EOD performance PDF)
# Report off by default; enable with report_enabled + report_eod_auto_generate in config
reportlab>=4.0.0
xlsxwriter>=3.2.9

# ── Transitive dependency pins (security fixes) ────────────────────────────────
# Pillow: transitive via reportlab. Pin to fix 20 high-severity CVEs
pillow>=12.3.0
# pypdf: direct/transitive dependency. 6.15.0 fixed CVE-2026-71852 + CVE-2026-71870.
# 6.16.1 fixes two more Dependabot-flagged moderate issues (large-memory
# /ToUnicode stream handling; long-runtime/large-memory CID font width
# ranges - GitHub alerts #29/#30, opened 2026-08-08 against requirements-lock.txt)
pypdf>=6.16.2
# click: transitive (Flask/uvicorn/etc). 8.4.1 fixes CVE-2026-7246
click>=8.5.0
# soupsieve: transitive via beautifulsoup4. 2.8.4 fixes GHSA-836r-79rf-4m37
soupsieve>=2.9.2
# setuptools: build tooling. 83.0.0 fixes GHSA-h35f-9h28-mq5c
setuptools>=83.0.0

# ── v2.44 Item 12 — News Sentinel (RSS feed parsing) ─────────────────────────
# Optional; only needed when news_sentinel_enabled = true
feedparser>=6.0.14

# ── v2.44 Item 20 — A/B Strategy Tester (Mann-Whitney U significance test) ───
# Optional; falls back to approx t-test if not installed
scipy>=1.11.0

# ── v2.45 Item 19 — Prometheus metrics export ────────────────────────────────
# Optional; only needed when metrics_enabled = true
# prometheus-client>=0.20.0

# ── Cryptography (v2.53+) ────────────────────────────────────────────────────
# Required for encrypted credential storage (infrastructure/security/credential_storage.py)
cryptography>=50.0.1

# ── Enterprise Web Dashboard (v2.53+) ──────────────────────────────────────────
# Required for the premium enterprise UI with auth, RBAC, config management
fastapi>=0.141.1
uvicorn>=0.52.4
jinja2>=3.1.0
authlib>=1.7.2
httpx>=0.28.0

# ── Live broker execution (optional — NOT needed for paper/manual trading) ────
# kiteconnect>=5.0.0
# pyotp>=2.9.0

# PostgreSQL adapter (DatabasePort implementation)
psycopg2-binary>=2.9

# MySQL adapter (DatabasePort implementation)
pymysql>=1.1.1

# Redis adapter (DatabasePort implementation)
redis>=8.1.0

# MongoDB adapter (DatabasePort implementation)
pymongo>=4.6

# DuckDB adapter (DatabasePort implementation)
duckdb>=1.5.5

# SQLAlchemy adapter (DatabasePort implementation)
sqlalchemy>=2.0.52

# Property-based testing for AsyncDbWriter
hypothesis>=6.100
