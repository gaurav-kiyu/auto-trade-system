# ── Node / JS (if any) ────────────────────────────────────────────────────────────
node_modules/

# ── Stray malformed git staging artifacts ────────────────────────────────────
.join*

# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.py[cod]
*.pyc
*.pyo
*.egg-info/
.eggs/
*.so
.tox/

# ── Editors / OS ──────────────────────────────────────────────────────────────
*.tmp
*.temp
# Runtime test fixtures (test_manual_signal_mode.py writes its MANUAL config here)
.tmp/
*~
*.swp
*.swo
*.bak
*.orig
.DS_Store
Thumbs.db
.idea/
.vscode/
.cursor/
.claude/

# ── Windows artifacts ─────────────────────────────────────────────────────────
nul
nul.*

# ── Environment / Secrets ─────────────────────────────────────────────────────
.env
.env.*
!.env.example
.env.local
json/config.json
json/config_v2.json
json/config.local.json
json/config.*.json
!json/config.template.json
secrets.json
secrets.enc
data/secrets.audit.jsonl
*.pem
*.key
*.crt

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
.python-version

# ── Test / Coverage ───────────────────────────────────────────────────────────
htmlcov/
coverage_html/
.coverage
coverage.xml
*.cover
.hypothesis/
.benchmarks/

# ── Build artifacts ───────────────────────────────────────────────────────────
dist/
build/
*.exe
*.spec

# ── Reports ───────────────────────────────────────────────────────────────────
reports/*.txt
reports/*.jsonl
reports/_tmp*
reports/*.json
reports/*.html
reports/*.pdf
reports/*.db
reports/*.sqlite
_regression_bad_stock_config.json

# ── Generated data files ────────────────────────────────────────────────────
db/*.db
db/*.sqlite
db/*.db-wal
db/*.db-shm
json/*.json
!json/index_config.defaults.json
json/*.jsonl
!json/config.template.json
!json/stock_config.defaults.json
!json/stock_config.json
!json/stock_config.template.json
!json/dashboard_config.json
!json/launcher_settings.json
!json/index_trader_gui_layout.json
!json/pre_implementation_allowlist.json
!json/success_metrics_trend.json
!data/.gitkeep
# Success-metrics trend history is governance evidence — tracked in git so the
# time-series survives CI checkouts and is provable across releases.
!json/success_metrics_trend.json

# ── Control plane audit ─────────────────────────────────────────────────────
logs/control_plane_audit.jsonl

# ── Logs ──────────────────────────────────────────────────────────────────────
logs/*.log
logs/*.log.*
logs/*.bak
logs/*.jsonl
logs/audit/
logs/metrics_report.json
tg_fallback_alerts.log
crash_recovery.log

# ── State / DB ────────────────────────────────────────────────────────────────
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.backup.*
json/trader_state.json
json/trader_state*.json
json/stock_trader_state.json
json/stock_trader_state.json.bak
json/ab_state.json
backups/

# ── Test artifact databases ─────────────────────────────────────────────────
test_recon_*.db
*_test.db
test_*.sqlite
nonexistent_*.db
nonexistent_*.sqlite

# ── Python cache (all levels) ────────────────────────────────────────────────
**/__pycache__/
*.pyc
*.pyo

# ── Generated reports & temp artifacts ───────────────────────────────────────
reports/backtest_results.json
reports/*.csv
config_audit.log
json/audit_trail.jsonl

# ── Trade debug / analysis scripts ───────────────────────────────────────────
debug_dd.py
check2.py
check_sigs.py
check_trades_db.py
live_data_test.py
live_signal_test.py
live_smoke_test.py
analyze_backtest.py
full_sim_test.py
run_analysis.py
text.txt
test.txt

# ── Presentation deliverables (generated) ────────────────────────────────────
docs/*.pdf
docs/*.pptx

# ── Generated decision graph export ───────────────────────────────────────────
# docs/decision_graph.json is a committed evidence artifact (ADR decision graph)
# generated by scripts/test_adr_e2e.py and tracked for constitution scoring.

# ── Secrets / Migration ──────────────────────────────────────────────────────
secrets_migration*.py

# ── ML models (generated at runtime) ──────────────────────────────────────────
models/*
!models/.gitkeep

# ── Audit report (generated) ──────────────────────────────────────────────────
FORENSIC_AUDIT_REPORT.md
ZERO_TRUST_CERTIFICATION_REPORT.md

# ── Pip installer bootstrap ───────────────────────────────────────────────────
get-pip.py

# ── Generated benchmark cache ─────────────────────────────────────────────────
json/benchmark_cache.json
benchmark_cache*.json

# Generated audit/certification reports (per-run)
/ZERO_TRUST_CERTIFICATION_REPORT.md
/FORENSIC_AUDIT_REPORT.md

# Legacy migration scripts (never re-add)
secrets_migration*.py

# Large build binaries
/OPBuying_INDEX_Launcher.exe
/dist/OPBuying_INDEX_Launcher.exe  

# Remove problematic re-inclusion of db/*.db
# (Already covered by *.db above; ensure no !db/*.db exception)

# Generated certification reports
FINAL_SYSTEM_SCAN_REPORT.md
ZERO_TRUST_FINAL_CERTIFICATION.md
PRODUCTION_READINESS_REPORT.md
*.report.md

# Generated documentation artifacts
*.pptx
*.pdf
# Review deliverables (regenerated by scripts/generate_review_artifacts.py) stay tracked
!docs/review/SYSTEM_REVIEW_SUMMARY.pdf
!docs/review/ARCHITECTURE_OVERVIEW.pptx

# ── Auth database (generated at runtime) ───────────────────────────────────────
db/auth.db
db/auth.db-wal
db/auth.db-shm

# ── Runtime audit artifacts ───────────────────────────────────────────────────
json/config_audit.jsonl
_score_snapshot.json

# Old/backup files
*.old

# Runtime incident tracking
json/incidents.json

# Font files - should be loaded from CDN
static/webfonts/

# Continuous Intelligence runtime history
json/continuous_intelligence_history.jsonl
bandit_report.json
docs/System_Summary_Report.pdf
docs/Architecture_Overview.pptx
uploads/
# Runtime evidence caches (regenerated by scripts)
_score_data.json
_pr_data.json
_score_snapshot.json
logs/*.json
reports/paper_trading/

# Playwright MCP test-session snapshots (ad-hoc UI verification, not build output)
.playwright-mcp/

# ── OPB Production Runtime / EC2 ───────────────────────────────────────────
/.env
/.env.*
!/.env.example
!/env.*.example

/venv/
/.cache/
/logs/
/db/
/backups/

/*.db
/*.sqlite
/*.sqlite3

# Runtime-generated presentation/output artifacts
/reports/presentations/
/reports/paper_trading/
/reports/backtests/
/reports/*.pptx
/reports/*.pdf

# Local browser/test artifacts
/playwright-report/
/test-results/
/screenshots/

# Local machine state
/.local/
/runtime/
/tmp/

# Credentials / deployment secrets
/k8s/secret.yaml
/k8s/*secret*.yaml
*.pem
*.key
*.p12
*.pfx

