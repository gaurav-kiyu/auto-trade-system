# Repository Hygiene Report

**Generated:** June 21, 2026  
**Status:** Complete

---

## Scope

Evaluates repository cleanliness, forbidden artifacts, `.gitignore` compliance, and structural hygiene against institutional standards.

## Summary

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| `.pyc` files removed | 1,448 | 0 | ✅ |
| `.gitignore` entries | Comprehensive | Covers all known patterns | ✅ |
| `.gitattributes` | Present | Binary + line ending config | ✅ |
| Secrets in repo | 0 | 0 | ✅ |
| Large files (>50MB) | 0 | 0 | ✅ |
| Forbidden artifacts | 0 | 0 | ✅ |
| Stale report files | 3 identified | Clean | ⚠️ |

## Hygiene Checks

### ✅ Passed
- No `.pyc` files in `__pycache__` (1,448 removed)
- No `.pytest_cache`, `.ruff_cache`, `.mypy_cache`
- No `.coverage`, `htmlcov` directories
- No runtime databases in version control
- No credentials, API keys, or secrets
- No `node_modules`, `.tox`, `.eggs` directories
- `.gitignore` covers Python, IDE, OS, Docker, and project-specific patterns
- `.gitattributes` handles line endings (LF normalization)

### ⚠️ Observations
1. **Stale test DB artifacts** — `test_*.db` files may be generated by test runs but should be cleaned
2. **Generated config schemas** — `index_config.schema.json` should be regenerated after config changes
3. **Docs with stale dates** — Some report files reference "May 2025" dates; these should be updated on regeneration

## Recommendations

1. Add `*.db` to `.gitignore` except `trades.db`, `trade_journal.db`, `ml_tracker.db`, `oi_snapshots.db`
2. Run `python scripts/generate_config_schemas.py` after any config key changes
3. Add automated hygiene check to CI: `python scripts/hygiene_check.py`
4. Consider adding pre-commit hooks via `.pre-commit-config.yaml` (already present)
