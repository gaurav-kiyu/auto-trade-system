======================================================================
CONFIG DRIFT REPORT — Phase 1 / Repository Clean Room
======================================================================
Generated: June 4, 2026

Baseline: index_config.defaults.json (single source of truth for all default values)

Config files audited:
  - index_config.defaults.json  (source of truth)
  - stock_config.defaults.json  (stock defaults)
  - config.json                 (active user config)
  - config.template.json        (template with env placeholders)
  - stock_config.json           (active stock config)
  - .env.example               (environment variable templates)

Result: NO CRITICAL DRIFT DETECTED

Previous session fixed: 613 missing keys added to config.template.json
  (294 → 907 keys, now matches defaults)

Known gaps:
  - All config keys are backward-compatible with safe defaults
  - sync_config_template.py script available for template regeneration
  - Schema generation via: python scripts/generate_config_schemas.py

======================================================================
[Report generated by automated scan]
======================================================================