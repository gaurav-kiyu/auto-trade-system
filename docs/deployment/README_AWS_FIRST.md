# Deployment Target: AWS

The production target for this release is **AWS EC2**, not Azure.

Use `AWS_EC2_PRODUCTION_RUNBOOK.md` and `scripts/aws_preflight.sh` for the first deployment. The Azure files remain in the repository as an alternative deployment path and are not part of the AWS activation procedure.
