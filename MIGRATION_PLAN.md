# Migration Plan — OPB v2.53.x → v2.54.0

**Generated:** 2026-06-25
**Target Version:** v2.54.0

---

## 1. Overview

This document describes the steps required to migrate from OPB v2.53.x to v2.54.0. Migration is designed to be **backward-compatible** — all existing configurations, databases, and strategies will continue to work without changes.

## 2. Breaking Changes

**No breaking changes in v2.54.0.** All changes are additive:
- New modules added without modifying existing APIs
- Deprecated modules retain their interfaces (DeprecationWarning added)
- Config keys are optional with safe defaults

## 3. Configuration Changes

| Config Key | Type | Default | Notes |
|------------|------|---------|-------|
| No new required keys | — | — | All new features have safe defaults |

## 4. Database Migration

| Database | Change | Migration Required | Rollback |
|----------|--------|-------------------|----------|
| trades.db | None | No | N/A |
| trade_journal.db | None | No | N/A |
| ml_tracker.db | None | No | N/A |
| oi_snapshots.db | None | No | N/A |
| execution_state.db | None | No | N/A |

## 5. Migration Steps

### Step 1: Backup
```bash
python scripts/backup_databases.py --retain 30
```

### Step 2: Install New Dependencies
```bash
pip install -r requirements.txt
```

### Step 3: Verify Config Compatibility
```bash
python -m core.version_compatibility --json
```

### Step 4: Run Certification Gates
```bash
python -m core.certification.gate
```

### Step 5: Run Tests
```bash
python -m pytest tests/ -q
```

### Step 6: Start in Paper Mode
```bash
python index_app/index_trader.py --paper
```

### Step 7: Monitor for 1 Trading Session
- Verify signal generation
- Verify paper fills
- Verify no errors in logs

### Step 8: Switch to Shadow/Production
```bash
python index_app/index_trader.py  # shadow mode
```

## 6. Verification Checklist

| Check | Command |
|-------|---------|
| All tests pass | `python -m pytest tests/ -q` |
| Certification gates pass | `python -m core.certification.gate --json` |
| Architecture compliance | `python scripts/check_architecture_compliance.py --ci` |
| Version compatibility | `python -m core.version_compatibility --json` |
| Capacity planning | `python -m core.capacity_planning --json` |
| SLO compliance | `python -m core.slo_governance --check --json` |

## 7. Duration

| Step | Estimated Time |
|------|---------------|
| Backup | 1 minute |
| Install dependencies | 2 minutes |
| Verify config | 30 seconds |
| Run certification gates | 10 seconds |
| Run tests | 5 minutes |
| Paper trading validation | 1 trading session |
| **Total** | **~10 minutes + 1 trading session** |

## 8. Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|:----------:|:------:|------------|
| Config key mismatch | Low | Low | All new keys have safe defaults |
| DB schema incompatibility | Low | Medium | Backup before migration |
| Runtime error in new code | Low | Medium | Paper mode validation first |
| Regression in existing feature | Low | High | Full test suite validation |

*Generated by Codebuff AI — June 25, 2026*
