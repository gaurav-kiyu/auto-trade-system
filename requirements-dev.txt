# OPB Dev / CI — install after requirements.txt
# Usage: pip install -r requirements.txt -r requirements-dev.txt
#        pytest tests/ -q          (554 tests, ~2 min)

# Test runner
pytest>=8.0
pytest-timeout>=2.3.0  # per-test timeout guard used by CI (--timeout=N)
pytest-xdist>=3.6  # parallel test execution: pytest tests/ -n auto

# FastAPI/Starlette TestClient backend (starlette.testclient requires httpx)
httpx>=0.28.0

# Schema validation (used by test_config_schema.py)
jsonschema>=4.20

# ML deps — needed for test_ml_classifier.py
lightgbm>=4.0.0
scikit-learn>=1.3.0

# Reporting deps — needed for test_report_generator.py
reportlab>=4.0.0

# PPTX generation — core/presentation_generator.py uses RGBColor at module level
python-pptx>=1.0.2

# Linter (optional)
# python -m ruff check index_app core scripts tests
ruff>=0.4

# PostgreSQL adapter (DatabasePort implementation)
psycopg2-binary>=2.9

# MySQL adapter (DatabasePort implementation)
pymysql>=1.1.1

# Redis adapter (DatabasePort implementation)
redis>=5.0

# MongoDB adapter (DatabasePort implementation)
pymongo>=4.6

# DuckDB adapter (DatabasePort implementation)
duckdb>=1.0

# SQLAlchemy adapter (DatabasePort implementation)
sqlalchemy>=2.0

# Property-based testing for AsyncDbWriter
hypothesis>=6.100
PyYAML>=6.0
pytest-asyncio>=1.4.0

# ── Dev-only security floors (not runtime deps) ───────────────────────────────
# nltk: 3.10.0 fixes CVE-2026-12061/12074/12075/12243 (was 3.9.4)
nltk>=3.10.0
# mcp: 1.28.1 fixes CVE-2026-52869/52870/59950 (was 1.23.3)
mcp>=1.28.1
