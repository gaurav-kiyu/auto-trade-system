{
    "_NOTE_PROFILE": "PAPER profile — simulated trading with realistic capital values. Merge this on top of config.json at load time. PAPER mode generates all signals and simulates order fills but places NO real orders. Use this to validate strategy behaviour before switching to AUTO. Keys not listed here inherit from config.json.",

    "EXECUTION_MODE": "PAPER",
    "MANUAL_SIGNALS_ONLY": false,
    "BROKER_API_ENABLED": false,

    "TG_STARTUP_ALERT": true,
    "TG_QUIET_MODE": false,
    "TG_TRADE_ONLY": false,

    "AUDIT_LOG_ENABLED": true,
    "STRUCTURED_EVENTS_ENABLED": true,

    "AUTO_TUNE_ENABLED": false,
    "AUTO_TUNE_DRY_RUN": true,
    "AI_ENGINE_ENABLED": false,
    "AUTO_LEARNER_ENABLED": false,
    "ADAPTIVE_THRESHOLD_ENABLED": false
}
