# Performance Report — OPB v2.53.0

**Generated:** 2026-06-03

---

## 1. Runtime Performance

| Metric | Measurement | Notes |
|--------|-------------|-------|
| Python version | 3.14.4 | Compiled, optimized |
| Scan interval | 60s (configurable) | `SCAN_INTERVAL` |
| Trading loop cycle | ~55s (with yfinance) | Cache reduces to ~2s |
| yfinance data cache TTL | 60s | `_YF_CACHE_TTL` |
| Memory usage (idle) | ~150-200 MB | At rest (Python process) |
| Memory usage (trading) | ~200-300 MB | With positions and data frames |
| Startup time | ~20-30s | Config load + imports + DI wiring |
| Shutdown time | ~2-5s | Position save + WS disconnect |

## 2. Bottleneck Analysis

| Bottleneck | Location | Impact | Mitigation |
|------------|----------|--------|------------|
| yfinance data fetching | `_fetch_intraday_data()` | High latency (~10-15s per index) | Cross-cycle caching implemented |
| NSE holiday API | `_fetch_nse_holidays_dynamic()` | Network dependent | Fallback set for 2026 |
| Signal generation | `_generate_trading_signal()` | CPU-bound (ML inference) | LightGBM optimized |
| SQLite writes | Multiple modules | Disk I/O | WAL mode enabled via `db_utils.py` |
| Telegram dispatch | `telegram_queue.py` | Rate-limited | Queue-based with rate limiting |

## 3. Concurrency

| Aspect | Implementation | Status |
|--------|---------------|--------|
| Thread safety | Lock-based (`_state_lock`, `_pos_lock`, `_bos_lock`) | ✅ |
| Deadlock prevention | Lock ordering enforced (state → perf → history → ac) | ✅ |
| Thread pool | `ThreadPoolExecutor` for Telegram + WS | ✅ |
| ASync operations | None — synchronous with `_shutdown.wait()` | ⚠️ Consider async for I/O |

## 4. Recommendations

| # | Recommendation | Impact | Effort |
|---|---------------|--------|--------|
| 1 | Add connection pooling for SQLite | Medium | Small |
| 2 | Cache NSE holiday data longer (weekly refresh) | Low | Trivial |
| 3 | Move yfinance fetches to background thread | Medium | Medium |
| 4 | Add async option for broker calls | High | Large |
| 5 | Profile ML inference latency per index | Low | Small |

## Overall: ✅ ACCEPTABLE for current scale

*Generated by Codebuff AI — June 3, 2026*
