"""Config Domain — configuration loading, validation, and lifecycle management.

Extracted from index_trader.py ``_load_config()``, ``_set_config_fail_safe()``,
and ``_notify_config_failure()`` to reduce the monolith and centralise
config-loading logic.
"""
